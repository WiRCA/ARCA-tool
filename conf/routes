# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page, links to: new-rca-case, view-rca-cases
GET     /                                       RCACase.index

#RCACase
GET     /RCACase                                RCACase.index
GET     /RCACase/create                         RCACase.create
#GET     /RCACase/{id}/ProblemDefinition         ProblemDefinition.index
#GET     /RCACase/{id}/ProblemDefinition/{id}/ProblemCause   ProblemCause.index
#GET     /RCACase/{id}/ProblemDefinition/{id}/ProblemCause/{id}/CorrectiveAction CorrectiveAction.index

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

# Catch all
*       /{controller}/{action}                  {controller}.{action}
